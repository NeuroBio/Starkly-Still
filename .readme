### Self
conversion steps:
Tag Italics:
- Press Ctrl + H.
- Click More >> -> Format -> Font -> Italic.
- In "Replace with," type <i>^&</i>.
- Click Replace All.

Be wary of where your cursor is when you change the format; you can use it for the search OR replace.

Bold is same but with <b>